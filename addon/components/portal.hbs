{{!-- using an autonomous custom element for semantic purposes only --}}
<portal-content name={{this.guid}} target={{this.target}} {{portal this.guid this target=@target}}>
  {{#-in-element this.targetElement}}
    {{yield this}}
  {{/-in-element}}
</portal-content>